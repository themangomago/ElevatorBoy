extends Node


enum PersonClass {
	Grandma = 0, 
	Businessman = 1,
	Student = 2,
	Employee = 3
}

enum PopupType { Money = 0, Rating }

enum PersonState {Waiting, Moving, Leaving}

enum ElevatorState {NoFloor, Offboarding, Onboarding}

enum Direction {Left, Right}
